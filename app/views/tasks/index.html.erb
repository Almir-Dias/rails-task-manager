<ol>   
  <% @tasks.each do |task| %>
    <li>
    <%= link_to task.title, task_path(task) %>
    
    
    <input type="checkbox" value="<%= task.completed ? 'true' : 'false' %>" <%= task.completed ? 'checked' : '' %> >

    

    </li>
  <% end %>
</ol>


